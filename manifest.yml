---
applications:
 - name: stackoverflow-searcher
   random-route: true
   memory: 64M
   instances: 1
   buildpack: https://github.com/cloudfoundry/staticfile-buildpack.git
   env:
   FORCE_HTTPS: true
